name: Security Scan - NutriApp

# Principio de mínimo privilegio
permissions:
  contents: read
  pull-requests: read

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - name: Secret Detection
        run: echo "Secret scanning would run here"